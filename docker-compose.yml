services:
  bot:
    build: .
    env_file:
      - .env
    volumes:
      - ./bookclub.db:/app/bookclub.db
      - ./bookclub_bot.log:/app/bookclub_bot.log
      - ./bot_persistence:/app/bot_persistence
    restart: always
