services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/bookclub.db
      - PERSISTENCE_PATH=/app/data/bot_persistence
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: always
